{
  "name": "entiq",
  "private": true,
  "version": "0.1.0",
  "description": "EnTIQ — one platform, one login, one client record. Modules sold on top.",
  "packageManager": "pnpm@9.15.4",
  "engines": { "node": ">=20.19", "pnpm": ">=9" },
  "scripts": {
    "dev": "pnpm --filter @entiq/web dev",
    "build": "pnpm -r --filter './packages/**' build && pnpm --filter @entiq/web build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf dist node_modules && rm -rf node_modules"
  }
}
